{
    "name": "@naverpay/changeset-actions/canary-publish",
    "description": "canary publish action based on changeset",
    "sideEffects": false,
    "version": "0.0.1",
    "main": "./dist/index.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npx ncc build src/index.ts -t",
        "clean": "rm -rf dist"
    },
    "dependencies": {
        "@changesets/read": "^0.6.0",
        "@vercel/ncc": "^0.38.1",
        "fast-glob": "^3.3.2",
        "resolve-from": "^5.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.14.9"
    }
}
